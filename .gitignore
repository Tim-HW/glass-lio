.vscode

# colcon artefacts, if anyone builds from inside the package rather than the workspace root
build/
install/
log/
compile_commands.json

# One-off M3DGR benchmark tooling -- specific to evaluating this one dataset, not
# project infrastructure. Kept locally, not versioned.
scripts/eval/
.venv-eval/

# Local-markdown issue tracker (docs/agents/issue-tracker.md) -- specs and tickets
# stay on this machine, never in the public repo.
.scratch/

# Local Claude settings and task state
.claude/
